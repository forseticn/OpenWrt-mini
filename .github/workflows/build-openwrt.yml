name: Build OpenWrt

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v2
      with:
        repository: coolsnowwolf/lede
        submodules: 'recursive'

    - name: Install Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential asciidoc binutils bzip2 gawk gettext git libncurses5-dev libz-dev patch python3 python2.7 unzip zlib1g-dev lib32gcc1 libc6-dev-i386 subversion flex uglifyjs git-core gcc-multilib p7zip p7zip-full msmtp libssl-dev texinfo libglib2.0-dev xmlto qemu-utils upx libelf-dev autoconf automake libtool autopoint device-tree-compiler g++-multilib

    - name: Check Disk Space
      run: |
        df -h
        echo "Free Disk Space:"
        free -h

    - name: Use Custom Configuration
      run: cp ${{ github.workspace }}/.config ./.config

    - name: Clean Failed Downloads
      run: make download -j8 V=s || true
      # 这个命令尝试下载所有预先定义的源，如果有下载失败，它会被删除并在下次构建时重新下载

    - name: Compile OpenWrt
      run: |
        ./scripts/feeds update -a
        ./scripts/feeds install -a
        make -j8 download V=s
        make -j$(nproc) V=s

    - name: Upload Artifacts
      uses: actions/upload-artifact@v2
      with:
        name: openwrt-binaries
        path: bin/

    - name: Create Release
      id: create_release
      uses: actions/create-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ github.ref }}
        release_name: Release ${{ github.ref }}
        draft: false
        prerelease: false

    - name: Upload Release Asset
      uses: actions/upload-release-asset@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: ./bin/packages
        asset_name: openwrt-packages.zip
        asset_content_type: application/zip
